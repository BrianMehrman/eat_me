<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first %><br />
    <%= f.text_field :first %>
  </div>
  <div class="field">
    <%= f.label :last %><br />
    <%= f.text_field :last %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label "My Avatar" %><br />
	<%= image_tag(@user.avatar_url) if @user.avatar? %>
    <%= f.file_field :avatar %>
	<%= f.hidden_field :avatar_cache %>
	</p>
  </div>
  <div class="field">
    <%= f.label :propGroupId %><br />
    <%= f.number_field :propGroupId %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
